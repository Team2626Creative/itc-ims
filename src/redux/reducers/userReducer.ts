const initialState = {

}


export const usersReducers = (state = initialState, action:{type:string,payload?:object}) => {
    switch (action.type){
        
        default:
            return state;
    }
}   
